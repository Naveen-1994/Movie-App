import { useSelector } from 'react-redux'
import React from 'react'
import MovieContainer from './movie-components/movieContainer'

function App() {
  return (
    <div>
      <MovieContainer />
    </div>
  );
}

export default App;
